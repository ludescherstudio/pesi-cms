# ── pesi runtime artifacts — NEVER commit these ──────────────
# Backups are byte-for-byte copies of your page source. Committing one to a
# public repo leaks the full PHP source including PESI_PASSWORD.
*.pesi-backup.*
.pesi-throttle

# Client uploads belong to the site, not to the pesi package.
uploads/

# Manual pre-change backups
*.old

# ── Maintainer-only, never published ─────────────────────────
# Private dev guide: names unreleased sibling projects and internal
# conventions. Public positioning lives in README.md instead.
CLAUDE.md
AGENTS.md

# ── Local tooling / editor config ────────────────────────────
.claude/
.vscode/
.idea/
*.code-workspace

# ── OS / sync junk ───────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
._sync_*.db
.owncloudsync.log
*.~lock.*
